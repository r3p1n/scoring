{
  "name": "scoring",
  "version": "1.0.8",
  "description": "App for calc score",
  "scripts": {
    "dev": "vite --mode dev",
    "build": "vite build --mode preprod",
    "build_with_prefix": "vite build --base=/scoring/ --mode prod",
    "serve": "vite preview",
    "deploy": "surge --domain scoring-for-fun.surge.sh"
  },
  "license": "MIT",
  "devDependencies": {
    "surge": "^0.23.1",
    "vite": "^3.0.0",
    "vite-plugin-solid": "^2.3.0"
  },
  "dependencies": {
    "@solidjs/router": "^0.4.2",
    "@suid/icons-material": "^0.2.7",
    "@suid/material": "^0.6.2",
    "@suid/vite-plugin": "^0.0.2",
    "solid-js": "^1.5.4"
  }
}
